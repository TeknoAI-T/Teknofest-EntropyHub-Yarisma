[package]
name = "entropyhub-core"
description = "RK4-integrated Rössler chaotic system with OS-seeded state and a built-in Lyapunov-exponent self-check. No Python/pyo3 dependency -- usable from any Rust project."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
sha2 = "0.10"
getrandom = "0.2"

[dev-dependencies]
